---
name: reconciliation
description: 对账供应商Excel账单，比对订单/票据状态和金额是否与系统一致。当运营提到"对账"、"核对账单"、"对一下账"、"帮我对下账"时触发。用户需先发Excel文件，Agent收到文件路径后调用 reconciliation 工具完成比对。
---

# 供应商账单对账

## 第一步：确认文件

检查消息中是否包含文件路径（格式为 `[文件: /path/to/file]`）。

- **有文件路径** → 提取路径，进入第二步
- **没有文件** → 回复：

  > 请先发送供应商账单文件（Excel 格式），再发送对账指令。

---

## 第二步：告知开始处理

**立即**回复以下内容（先发出，给用户反馈）：

> 正在对账，请稍候……

然后调用 **reconciliation** 工具，传入 `file_path`（从第一步提取的路径）。

---

## 第三步：渐进式输出对账报告

拿到工具返回的 JSON 后，**按以下顺序逐段输出**，让用户边看边等：

### 3.1 汇总（第一段，先输出）

```
📊 对账完成 — {supplier}
──────────────────────────
总计：{total} 条
✅ 匹配：{matched} 条
⚠️ 有差异：{issue_count} 条
❓ 系统未找到：{not_found_count} 条

💰 财务汇总
总销售金额：¥{total_sales_amount}
佣金（{commission_rate*100}%）：¥{commission}
应打款供应商：¥{payout}
```

### 3.2 差异明细（第二段）

若 `issue_count > 0`，逐条列出：

```
【差异明细】

1. 订单号：{order_no}
   {若是路路出行/车盈网，附加：乘车人：{passenger}  条码：{barcode}}
   - {field}：供应商 {supplier_val} → 系统 {db_val}
   （同一条目有多个差异字段时逐行列出）

2. 订单号：...
   ...
```

若差异超过 20 条，前 20 条后补充：

```
…… 还有 {剩余数} 条差异，如需继续查看请告知。
```

### 3.3 未找到（第三段，最后输出）

若 `not_found_count > 0`：

```
【系统中未找到的订单/条码】
{逐行列出，每行一个，最多显示 10 条}
{若超过 10 条：…… 共 {not_found_count} 条未找到}
```

### 3.4 全部匹配时

若 `issue_count == 0` 且 `not_found_count == 0`：

```
✅ 全部匹配，账单数据与系统完全一致，无差异。
```

---

## 第四步：等待追问

报告输出后保持上下文，等待运营追问。常见场景：

- **"第X条详情"** / **"查一下这个订单"** → 用 `mysql_query` 查询 `lulu_order` / `lulu_ticket` 详情展示
- **"为什么金额不一样"** → 查询订单详情后分析原因（优惠券、退款记录等）
- **"继续看差异"** → 输出第 21 条起的差异明细
- **"未找到的原因"** → 尝试用 `mysql_query` 查询该订单号是否存在于系统，反馈查询结果
